apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
namePrefix: ""
commonAnnotations:
  muxcore.media/overlay: dev
# Dev overlay keeps MUXCORE_INSECURE_DISABLE_TLS=true from base for local clusters.
