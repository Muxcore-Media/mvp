1. Create the auth secret (if not using an external IdP):
   kubectl -n {{ .Values.namespace }} create secret generic {{ .Values.auth.existingSecret }} \
     --from-literal={{ .Values.auth.passwordKey }}=CHANGE_ME

2. Images default to git.zem.systems/muxcore/* (Forgejo/LAN OCI) — see docs/PUBLIC-INSTALL.md.
   Override registry/coreTag via values; GHCR mirror is optional (docker-compose.ghcr.yml).

3. Mesh address for sidecars is Service DNS muxcored:9090 in namespace {{ .Values.namespace }}.

4. Prefer Kustomize overlays under deploy/kustomize/ when iterating on raw manifests;
   this chart tracks the same minimal platform slice.
