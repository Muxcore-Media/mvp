# GitHub Actions runner — retired

MuxCore origin CI is **Forgejo** on vault. See [`FORGEJO-RUNNER.md`](FORGEJO-RUNNER.md).

GitHub Actions / `admin:org` / `github-actions-runner` on gringotts are not used. GitHub will exist later only as the **public** consumer mirror (homelabbers), fed from Forgejo origin.
